using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class UpdateState : MonoBehaviour
{
	public GameObject firstPanel;
	public GameObject secondPanel;
	public GameObject threePanel;
	// Start is called before the first frame update
	void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
	
		//	firstPanel.SetActive(DirectorManager.getInstance().getSceneState() == 1);
		//	secondPanel.SetActive(DirectorManager.getInstance().getSceneState() == 2);
		//    threePanel.SetActive(DirectorManager.getInstance().getSceneState() == 3);



	}
}
